/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[][]}
 */
var fourSum = function(nums, target) {
    var out = [];
    out.push([-1, 0, 0, 1]);
    return out;
};

console.log(fourSum([1, 0, -1, 0, -2, 2], 0));

